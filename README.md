# Permutations
The goal is to print all possible rearrangements of a string, in alphabetical order (we are assuming that the string is already in alphabetical order and consists of different characters). We succeed that through a recursive function) 
